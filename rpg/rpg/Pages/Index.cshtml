@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

    <div class="text-center">
        <h1>@_logic.player.CurrentRoom.Name</h1>
        @foreach(Room room in _logic.player.CurrentRoom.TravelPsbl)
        {
            <a class="btn btn-primary">@room.Name</a>
        }
    </div>
